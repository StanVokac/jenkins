pipeline {
    agent any

    environment {
        NAME = "Stanfa"
    }

    stages {
        stage("Datascientest Variables") {
            steps {
                echo "School = ${env.NAME}""
            }
        }
    }
}